# Sublime rewrites font_size into User Preferences on zoom; strip it so it is never tracked
Preferences.sublime-settings filter=strip-sublime-fontsize

# pi writes defaultThinkingLevel into settings.json at runtime; strip it so it is never tracked
pi/.pi/agent/settings.json filter=strip-pi-thinkinglevel
