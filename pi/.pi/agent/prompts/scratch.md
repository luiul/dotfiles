---
description: Write text output to a scratch file and print the path
argument-hint: "<what to write>"
---

Produce the requested content, then:

1. Write it to `~/scratch/<descriptive-name>.md` (kebab-case, e.g. `incident-timeline-summary.md`).
2. Print the full absolute path so I can click to open it.
3. Also show the content in chat.

For follow-up edits to the same piece of text, update the same file rather than creating a new one.

If no request was provided below, ask me what to write.

Request:
$ARGUMENTS
